CLAUDE.md,spec/README.mdを起点として必要事項を確認後、spec/tasks内の[prefix]-*$ARGUMENTS*.md の実装に取り組んで下さい。

作業は、該当するブランチ(無ければ妥当なブランチを作成)で、git worktree(無ければ作成)内で行います。

ステップ一つが完了する毎にtasksを更新し、commit。次の作業に移る前に残りのcontextを確認し、次の作業の完了までにcompactが必要になってしまいそうならその時点で作業を中断して下さい